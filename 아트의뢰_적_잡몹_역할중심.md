# 아트 의뢰서 — 적 잡몹 (역할·기능 중심)

> 게임: `undead-lord` (Godot 4.6 · 모바일 세로 480×960)
> 용도: 전투 화면에 **떼로 몰려오는 잡몹**. 성을 향해 전진하다 하인(아군)·성을 공격한다.
> 작성일: 2026-06-18 · **이 의뢰서는 세계관·컨셉을 의도적으로 배제한다.** 종족·테마·서사는 작가 재량 — 단, **역할이 실루엣으로 한눈에 읽히는 것**이 절대 우선.
> 기능 정본(밸런스·행동): [MainPlay/05_보스/README.md](./MainPlay/05_보스/README.md) · 컨셉 버전(가제 명명 포함): [아트의뢰_Ch1_적_보스.md](./아트의뢰_Ch1_적_보스.md)

---

## 0. 이 문서의 전제 — "컨셉 없이, 역할로"

- **무엇을 그릴지(종족·세계관)는 지정하지 않는다.** 작가가 톤·테마를 자유롭게 잡아도 된다.
- **반드시 충족할 것은 "기능 가독성"** 하나다: 화면에 여러 마리가 겹쳐 있을 때, **크기·실루엣·색만으로 그 적의 역할(=위협의 종류)이 즉시 읽혀야** 한다.
- 톤 합의가 필요하면 컨셉 버전 의뢰서([아트의뢰_Ch1_적_보스.md](./아트의뢰_Ch1_적_보스.md))의 "동네 초보 모험가" 결을 참고하되, **이 문서만으로도 발주 가능**하도록 기능만으로 적었다.

---

## 1. 제작 방식 ⚠️ (전 캐릭터 공통)

> **본(Bone) 리깅 ❌ → 낱장 PNG 프레임 시퀀스로 납품.** 엔진은 리그를 import하지 않는다.

| 방식 | 툴 예시 | 비고 |
|---|---|---|
| A. 프레임 직접 작화 | Aseprite, Photoshop, Procreate, Clip Studio | 전통 프레임 애니 |
| B. 리그로 만들고 → PNG로 굽기 | Spine, DragonBones, Live2D, Blender(2D) | 내부 작업은 OK. **"PNG 프레임 시퀀스로 bake" 필수** |

---

## 2. 이미지 규격 (전 캐릭터 공통)

| 항목 | 요구사항 |
|---|---|
| **캔버스 크기** | **900 × 900 px** (전 프레임·전 타입 동일, 고정) |
| **포맷** | **PNG · RGBA 32bit · 투명 배경** |
| **스타일** | **치비(2~3등신) · HD 스무딩**(픽셀아트 ❌, 부드러운 셀/벡터 톤) — 하인·주인공·보스와 단일 화풍 |
| **제출 방식** | **프레임 개별 파일** (스프라이트시트 ❌) |
| **정렬** | 전 프레임 캔버스 **중심 기준 동일 피벗** — 발/중심이 프레임마다 튀지 않게 |
| **여백** | 캐릭터는 캔버스의 약 60~70% (공격 시 무기·팔 뻗음까지 안 잘리게) |
| **방향** | **측면(3/4) 1방향만** — 좌우 반전은 코드 처리(`flip_h`), 좌향 에셋 불필요 |
| **fps** | **15fps 고정** |

---

## 3. 역할·기능 스펙 (코드 정본 — `Enemy.gd:TYPE_PRESETS`)

> 아래 수치는 **현재 가밸런스**(`BASE_HP 50` · `BASE_SPEED 60` · `BASE_DAMAGE 10` 에 타입 배수 적용). 수치 자체보다 **상대 관계**(누가 크고·느리고·단단한가)를 디자인에 반영하는 게 목적.

| 코드 타입 (=폴더) | 역할 한 줄 | HP | 이동속도 | 피해 | 인게임 크기\* | 공격 폴더 | 특이 행동 |
|---|---|---|---|---|---|---|---|
| `enemy_normal` | **기준점** (표준 근접) | 50 | 60 (1.0×) | 10 | **1.0** (기준) | `Slashing` | 가장 평범. 다른 넷을 읽는 기준점 |
| `enemy_scout` | **후열 견제** (원거리) | 25 | 60 (1.0×) | 6 | **0.8** (작음) | `Shooting`(9프레임) | 사거리 **150**에서 활로 사격(↓§3.1). 전선·성을 멀찍이서 때려 수동 방어를 무력화. 약함 |
| `enemy_brute` | **방어선 고착** (중장 탱커) | **175** | **30 (0.5×)** | **15** | **1.45** (가장 큼) | `Slashing` | 느리지만 압도적으로 단단하고 한 방이 큼. "움직이는 벽" |
| `enemy_swarm` | **물량 압박** (다수 약체) | **15** | **78 (1.3×)** | 5 | **0.65** (가장 작음) | `Kicking` | 가장 약하고 빠름. **떼로** 몰려와 수로 압박 |
| `enemy_runner` | **방어선 돌파** (돌격병) | 30 | **102 (1.7×)** | 12 | 0.9 | `Slashing` | **가장 빠름. 하인을 무시하고 성으로 직행**(`ignore_minions`) |

\* 인게임 크기는 코드 자동(`Enemy.gd:BASE_SPRITE_SCALE 0.246` × 타입 `scale` 배수). **전부 같은 900×900 캔버스에 그리되**, 화면에선 위 배수로 축소/확대 표시 → **체구(질량감) 자체를 상대 비율에 맞게 설계**해야 자연스럽다(탱커=육중, 다수약체=잗다람).

### 3.1 사수(`enemy_scout`) = 후열 견제형 — 포즈에 반영
- 사수의 역할은 **전선에 닿기 전·전선 밖 사거리(150)에서 아군·성을 사격**하는 것. 근접으로 막히는 다른 적과 달리 **수동 방어로는 안 막혀, 플레이어가 능동적으로 끊어야 하는 우선 처치 대상**이다.
- → **공격(`Shooting`)은 멀찍이서 활을 당기는 원거리 자세.** 칼을 휘두르는 근접 느낌 ❌. "쏘는 적"이라는 게 자세만 봐도 읽혀야 한다.
- ※ 과거 "아군 궁수를 노려 다이브"(EN8 궁수 사냥꾼)는 **폐기** — 잡몹 단계 단순화(↓부록 코드 후속).

### 3.2 돌격병(`enemy_runner`) — 신규 작화 선택
- 돌격병은 **현재 `enemy_normal` 아트를 재사용**(독립 폴더 없음). 신규 작화 **필수 아님**.
- 원하면 옵션으로 "질주하는 별도 실루엣"을 새 폴더(`enemy_runner/`)로 받을 수 있으나, **MVP는 normal 재사용으로 충분**. 우선순위 최하.

---

## 4. 역할별 실루엣 요구 ⚠️ (이 의뢰서의 핵심)

> 컨셉은 자유. 단, 아래 **"실루엣이 전달해야 할 것"**은 어떤 테마로 가든 반드시 충족. (작가가 종족·무기·색을 정하되, 이 읽힘을 깨면 안 됨.)

| 코드 타입 | 실루엣이 전달해야 할 것 | 피해야 할 오독 |
|---|---|---|
| `enemy_normal` | **"기본·중간"** — 무난한 인간형 근접 1체. 나머지를 가늠하는 기준 | 너무 화려해서 보스·정예로 오인되면 ❌ |
| `enemy_scout` | **"멀리서 쏜다"** — 호리호리·날렵, 손에 활(원거리 무기)이 분명. 가벼워 보임 | 근접 검사로 보이면 ❌ (원거리 위협이 안 읽힘) |
| `enemy_brute` | **"크고 단단하다"** — 압도적 덩치·중량감·방어구 밀도. 느릿한 무게 | 빨라 보이거나 날렵하면 ❌ |
| `enemy_swarm` | **"약하고 많다"** — 작고 잗다람. 한 마리는 사소하지만 떼로 위협 | 단단해 보이거나 크면 ❌ |
| `enemy_runner`(옵션) | **"제일 빠르다·앞만 본다"** — 질주 자세, 방어 무시하고 돌진 | 신중·방어적으로 보이면 ❌ |

- **색 구분 권장**: 동시에 여러 종이 겹치므로 타입별 주조색을 뚜렷이 달리해 군집 속 식별을 돕는다.
- **체구 = 1순위 식별자**: 코드 스케일과 별개로 캔버스 안 체형부터 탱커는 육중, 다수약체는 잗다랗게.

---

## 5. 애니메이션 세트 (5모션 · 타입당)

| 모션 | 프레임 | 재생 | 연기 디렉션 |
|---|---|---|---|
| **Idle** | 18 | 루프 ♻️ | 대기·경계 |
| **Walking** | 24 | 루프 ♻️ | 성을 향해 전진. 타입별 이동 무게 반영(탱커=느릿, 다수약체·돌격병=종종·질주) |
| **공격** | 12 (단 `enemy_scout`=**Shooting 9**) | 1회 | 폴더명 코드 고정 ↓ |
| **Hurt** | 12 | 1회 | 피격 움찔 |
| **Dying** | 15 | 1회 | 쓰러짐 |

### 공격 폴더명 = 코드 고정 (`Enemy.gd:TYPE_PRESETS`)
| 타입 | 공격 폴더 | 실제 동작 |
|---|---|---|
| `enemy_normal` / `enemy_brute` | `Slashing` | 근접 베기/내려치기 |
| `enemy_scout` | `Shooting` (9프레임) | **활 당겨 쏘기** (발사체는 코드가 그림 → 자세만) |
| `enemy_swarm` | `Kicking` | 발길질/내려치기 (근접) |

> **프레임 수는 권장값, 가감 가능.** 단 ① **000부터 연속 번호**(중간 빈칸 ❌) ② **모션별 최종 프레임 수를 명기해 전달**(말없이 변경 ❌) ③ **15fps 고정**, 1회 모션(공격·피격·사망)은 권장값 근처 유지.

---

## 6. 파일 네이밍 규칙 (필수 — 코드가 파일명으로 자동 로드)

```
[폴더]/                                예) enemy_brute/
├── Idle/      [접두]_Idle_000.png     ~ _017.png   (18장)
├── Walking/   [접두]_Walking_000.png  ~ _023.png   (24장)
├── Slashing/  [접두]_Slashing_000.png ~ _011.png   (12장)   ※ scout만 Shooting/ 9장, swarm은 Kicking/
├── Hurt/      [접두]_Hurt_000.png     ~ _011.png   (12장)
└── Dying/     [접두]_Dying_000.png    ~ _014.png   (15장)
```

- 경로 = `res://assets/characters/[폴더]/[모션]/[접두]_[모션]_[NNN].png`
- **접두**(prefix)는 폴더당 고정 문자열. **납품 시 폴더별 접두를 명기**해 전달 → 코드가 그 값으로 로드.
- 번호 **3자리 0패딩**, **000부터 연속**.
- 모션명 **대소문자 정확히 일치**(`Idle`/`Walking`/`Slashing`/`Shooting`/`Kicking`/`Hurt`/`Dying`).
- 같은 타입의 **외형 변형**(예: 표준 근접의 검사/창병/도끼병)을 넣고 싶으면 접두 변경 ❌ → **새 폴더**로 분리(예: `enemy_normal2/`). 화면이 풍성해지는 선택 옵션.

| 캐릭터 | 폴더 | 공격 폴더 | 신규 작화 |
|---|---|---|---|
| 표준 근접 | `enemy_normal` | `Slashing` | ✅ 필수 |
| 원거리 사수 | `enemy_scout` | `Shooting`(9프레임) | ✅ 필수 |
| 중장 탱커 | `enemy_brute` | `Slashing` | ✅ 필수 |
| 다수 약체 | `enemy_swarm` | `Kicking` | ✅ 필수 |
| 돌격병 | `enemy_runner` (옵션) | `Slashing` | ⬜ 선택 (없으면 `enemy_normal` 재사용) |

---

## 7. 화면 표시 컨텍스트 (참고)

- 해상도 **480 × 960** (모바일 세로). 잡몹은 화면에 **여러 마리 동시** 등장 → 실루엣·색을 뚜렷이.
- 인게임 스케일 = `Enemy.gd:BASE_SPRITE_SCALE 0.246 × 타입 scale 배수` → 900px 원본이 화면에선 축소 표시(표준 근접 ≈ 220px 안팎). **상대 비율이 자연스럽게 읽히도록 체구를 설계**.

---

## 체크리스트 (납품 전 확인)

- [ ] 900×900px 투명 PNG, 낱장 프레임 (시트 ❌)
- [ ] 신규 4종(`enemy_normal`/`enemy_scout`/`enemy_brute`/`enemy_swarm`): 5모션 폴더 + 프레임 수 일치 (Idle 18 / Walking 24 / 공격 12·scout 9 / Hurt 12 / Dying 15)
- [ ] 공격 폴더명 정확 (`Slashing` / `Shooting`(scout) / `Kicking`(swarm))
- [ ] **역할 실루엣 가독성**(§4): 탱커=크고 단단 · 사수=원거리 자세 · 다수약체=작고 많음 · 표준=기준
- [ ] 치비 HD 스무딩 단일 화풍(하인·주인공·보스와 통일)
- [ ] 폴더별 접두 명기해 전달 · 모션명 대소문자 일치 · 000부터 연속 번호
- [ ] 리그 파일이 아닌 **구운 PNG 프레임**으로 납품

---

## 부록 — 코드 후속 (내부용 · 아트팀 무관)

> 신규 에셋 입고 시점에 처리(Sonnet).

- `Enemy.gd:_get_char_prefix()` — 현재 `enemy_normal`/`enemy_brute` → `0_Bloody_Alchemist`, `enemy_scout`/`enemy_swarm` → `0_Forest_Ranger` 공유(임시 에셋). **4종 독립 폴더·접두로 분리** 필요.
- `enemy_runner`는 현재 `folder: "enemy_normal"`로 normal 아트 재사용(`Enemy.gd:TYPE_PRESETS`). 독립 작화 입고 시 폴더만 교체.
- **다이브 제거(EN9, 2026-06-18)**: 사수를 "후열 견제형(단순 원거리)"로 단순화 → `Enemy.gd:_find_target_archer` 호출(154~162행 인근)과 `_find_target_archer()` 정의를 제거하고, 사수도 일반 적처럼 근접 어그로 없으면 성으로 향하되 사거리 150에서 사격하도록 정리. 아트와 무관, 밸런스 트랙에서 처리.

## 관련 문서
- [MainPlay/05_보스/README.md](./MainPlay/05_보스/README.md) — Ch1 적 진영·역할 설계 정본(EN1~EN8)
- [아트의뢰_Ch1_적_보스.md](./아트의뢰_Ch1_적_보스.md) — **컨셉 포함** 통합 의뢰서(잡몹+보스, "동네 초보 모험가" 결)
- [아트의뢰_하인_4종.md](./아트의뢰_하인_4종.md) — 아군 하인 아트(동일 규격·화풍 기준)
