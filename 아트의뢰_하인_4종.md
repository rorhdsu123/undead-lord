# 아트 의뢰서 — 하인 4종 「마물 소환수」

> 게임: `undead-lord` (Godot 4.6 · 모바일 세로 480×960)
> 용도: 전투 화면 **하인(소환 미니언)** 4종. 기존 임시 에셋(`skeleton_warrior_1~3`) 교체.
> 작성일: 2026-06-04 (파일명 확정 2026-06-09) · **파일·폴더명 = 역할 영문**(`Warrior`/`Archer`/`Tank`/`Bomber`, §5 확정) · 종족(임프/고블린/슬라임/버섯)은 **비주얼 컨셉 가제**(인게임 명명은 세계관 확정 후 `Loc.gd`)

---

## 1. 캐릭터 컨셉 — 하인 4종 「마물 소환수」

### 한 줄 정의
> **마왕이 마법으로 불러내는 작고 귀여운 마물들.** 마왕성을 지키러 전선에 나서지만, 다들 한없이 진지하게 자기 역할을 수행한다.

### 세계관 정합 (참고)
- 하인 = **마왕이 소환하는 일회성 마물 소환수**. 전투에 직접 투입되고, 쓰러지면 사라진다.
- 성에서 행정·운영을 맡는 **계약 직원(옛 용사·비전투)** 과는 **별개 존재**다. (직원은 안 싸움 — 별도 의뢰.)
- 톤 = **블랙코미디**: 캐릭터는 전원 진지. 웃음은 *귀여운 마물이 진지하게 전쟁을 수행하는 상황의 부조리*에서. (자뻑·허세 ❌, 카메라 향한 윙크 ❌)

### 4종 역할 ↔ 종족 매핑 (가제)
> 역할별 **실루엣이 한눈에 구분**되는 게 최우선(전투 가독성). 종족명은 가제 — 컨셉 검토 후 교체 가능.

| 코드 타입 | 역할 | 종족(가제) | 디자인 키워드 | 인게임 상대 크기\* |
|---|---|---|---|---|
| `warrior` 전사 | 근접 전선(균형) | **임프(Imp)** | 작은 뿔 꼬마 악마, 짧은 검+작은 방패. 기본형·표준 실루엣 | **1.0** (기준) |
| `archer` 궁수 | 후방 원거리 | **고블린(Goblin)** | 호리호리·날렵, 큰 활/새총. 겁은 많아 보여도 진지하게 조준 | **0.85** (작음) |
| `tank` 탱크 | 전선 방벽 | **슬라임(Slime)** | 크고 둥근 젤리 몸체, 반투명·물컹. 피격을 몸으로 흡수하는 벽 | **1.45** (가장 큼) |
| `bomber` 폭탄병 | 돌진 자폭 | **버섯(Mushroom)** | 빵빵하게 부푼 포자 갓, 금방 터질 듯 불안정. 돌진해 자폭 | **0.85** (작음) |

\* 코드가 자동 적용(`SkeletonWarrior.gd:BASE_SPRITE_SCALE 0.246` × 위 배수). **전부 같은 900×900 캔버스에 그리되**, 슬라임은 화면에서 ×1.45로 가장 크게, 임프 기준, 궁수·폭탄병은 작게 표시됨 → **상대 비율이 자연스럽게 읽히도록 체구를 설계**(슬라임=육중한 벽, 폭탄병=작고 빠른 돌격).

---

## 2. 리깅 / 제작 방식 ⚠️ 가장 중요

> **본(Bone) 리깅 ❌ → 낱장 PNG 프레임 시퀀스로 납품.**
> 엔진은 리그(뼈대)를 import하지 않습니다. **리그 파일째로 주면 게임에 못 넣습니다.**

| 방식 | 툴 예시 | 비고 |
|---|---|---|
| **A. 프레임 직접 작화** | Aseprite, Photoshop, Procreate, Clip Studio | 전통 프레임 애니 |
| **B. 리그로 만들고 → PNG로 굽기** | Spine, DragonBones, Live2D, Blender(2D) | 내부 작업은 본 리깅 OK. **단 "PNG 프레임 시퀀스로 export(bake)" 필수.** 리그 파일은 엔진에 안 들어감 |

→ B를 쓰더라도 **런타임 스켈레탈 연동이 아니라 "구운 프레임 결과물"만 받습니다.**

---

## 3. 이미지 규격 (주인공 의뢰서와 동일)

| 항목 | 요구사항 |
|---|---|
| **캔버스 크기** | **900 × 900 px** (전 프레임·전 종족 동일, 고정) |
| **포맷** | **PNG · RGBA 32bit · 투명 배경** |
| **제출 방식** | **프레임 개별 파일** (스프라이트시트 ❌) |
| **스타일** | **치비(Chibi) 2~3등신** · **HD 스무딩**(픽셀아트 ❌, 부드러운 셀/벡터 톤) — 주인공과 동일 화풍 |
| **정렬** | 전 프레임 캔버스 **중심 기준 동일 피벗** — 발/중심이 프레임마다 튀지 않게 |
| **여백** | 캐릭터는 캔버스의 약 60~70%(공격 시 팔·무기 뻗음까지 안 잘리게). 슬라임처럼 큰 종도 캔버스 안에서 동일 규칙 |
| **방향** | **측면(3/4) 1방향만** — 좌우 반전은 코드 처리(`flip_h`), 좌향 에셋 불필요 |

---

## 4. 애니메이션 세트 (5종 · 종족당 총 81프레임 · 전부 15fps 기준)

> 하인은 **이동**하므로 주인공(4종, 고정)과 달리 **Walking이 필수**. 5종 모두 납품.

| 모션 | 프레임 수 | 재생 | 공통 연기 디렉션 |
|---|---|---|---|
| **Idle** (대기) | **18** | 루프 ♻️ | 적이 없을 때·사거리 안에서 대기. 진지하게 경계 |
| **Walking** (이동) | **24** | 루프 ♻️ | 적을 향해 전진. 종족 이동 특성 반영(임프=또박또박 / 슬라임=물컹 굴러 / 고블린=종종걸음 / 버섯=불안하게 돌진) |
| **Slashing** (공격) | **12** | 1회 | **공격 모션**(폴더명은 코드 고정 = `Slashing`, 실제 동작은 종족별로 다름 ↓) |
| **Hurt** (피격) | **12** | 1회 | 맞고 움찔. 진지함이 흔들리는 순간 |
| **Dying** (사망) | **15** | 1회 | 소환수가 스러져 사라짐. 비장보다 "퐁" 사라지는 가벼움 |

### 종족별 「Slashing」(공격) 동작 디렉션
| 종족 | `Slashing` 폴더에 담길 실제 동작 |
|---|---|
| 임프(전사) | 짧은 검 **근접 베기** |
| 고블린(궁수) | **활 당겨 쏘기** (발사체는 코드가 따로 그림 → 자세만) |
| 슬라임(탱크) | 몸통 **들이받기/내려치기** (근접) |
| 버섯(폭탄병) | **자폭 동작** — 부풀어 터지는 전조(폭발 이펙트는 코드가 그림, 캐릭터는 "터지기 직전 부푼 자세"까지) |

> **폭탄병 메모**: 돌진→접촉 시 자폭하는 유닛이라 인게임에서 Idle은 거의 안 보임. **Walking(돌진)·Slashing(자폭)** 이 핵심. Dying은 도중에 처치당했을 때만 재생.

> **프레임 수는 권장값, 가감 가능.** 단 3가지 준수:
> 1. **000부터 연속 번호** — 중간 빈칸 ❌ (끊김 발생)
> 2. **모션별 최종 프레임 수를 명기해 전달** — 코드에서 그 수에 맞춤 (말없이 변경 ❌ → 잘리거나 에러)
> 3. **15fps 고정** — 루프(Idle/Walking)는 자유, **공격·피격·사망(1회)은 권장값 근처 유지** 권장(게임플레이 타이밍 정합)

---

## 5. 파일 네이밍 규칙 (필수 — 코드가 파일명으로 자동 로드)

> **폴더·파일명은 종족 비주얼이 아니라 「역할 영문명」으로 고정**합니다 — `Warrior` / `Archer` / `Tank` / `Bomber`.
> (코드 타입과 1:1 일치 → 매핑 혼동 0. 임프/고블린/슬라임/버섯은 §1의 **비주얼 컨셉**일 뿐, 파일명에는 안 씀.
> 인게임에 보이는 이름은 코드가 `Loc.gd`에서 따로 처리하므로, 파일명은 안정적 식별자로만 쓰입니다.)

| 역할(=폴더명) | 비주얼 컨셉(§1) |
|---|---|
| **Warrior** | 임프 |
| **Archer** | 고블린 |
| **Tank** | 슬라임 |
| **Bomber** | 버섯 |

```
[역할폴더]/                              예) Warrior/
├── Idle/      0_[Role]_Idle_000.png     ~ _017.png   (18장)   0_Warrior_Idle_000.png …
├── Walking/   0_[Role]_Walking_000.png  ~ _023.png   (24장)   0_Warrior_Walking_000.png …
├── Slashing/  0_[Role]_Slashing_000.png ~ _011.png   (12장)   0_Warrior_Slashing_000.png …
├── Hurt/      0_[Role]_Hurt_000.png     ~ _011.png   (12장)   0_Warrior_Hurt_000.png …
└── Dying/     0_[Role]_Dying_000.png    ~ _014.png   (15장)   0_Warrior_Dying_000.png …
```

- 4개 폴더 = 4역할: `Warrior`, `Archer`, `Tank`, `Bomber` (**대소문자 정확히 일치**)
- 번호는 **3자리 0패딩**, **000부터 시작**
- 접두 `0_` 고정, 모션명 **대소문자 정확히 일치**(`Idle`/`Walking`/`Slashing`/`Hurt`/`Dying`)
- 기존 컨벤션과 동일 (`0_Skeleton_Warrior_…` 자리에 역할명만 교체)
- **접두 `0_`은 의미 없는 고정 리터럴**(에셋팩 잔재) — 변형 인덱스 아님. 같은 역할의 **2차 비주얼**을 추가할 땐 접두를 `1_`로 바꾸지 말고 **새 폴더**로 분리: 예) `Warrior2/` 안에 `0_Warrior2_Idle_000.png` (접두는 그대로 `0_[폴더명]`). 코드는 변형을 **폴더로** 구분하므로 폴더 매핑 한 줄만 추가하면 됨.

---

## 6. 화면 표시 컨텍스트 (참고)

- 게임 해상도 **480 × 960** (모바일 세로), 하인은 화면에 **여러 마리 동시** 등장(전선 형성)
- 인게임 스케일 = `0.246 × 종족배수` → 900px 원본이 화면에선 작게(임프 ≈ 220px 안팎) 표시
- **여러 마리가 겹쳐도 역할이 구분**되도록 실루엣·색을 뚜렷하게 (슬라임=크고 둥글게, 고블린=가늘게, 폭탄병=부푼 갓, 임프=뿔+검)

---

## 체크리스트 (납품 전 확인)

- [ ] 900×900px 투명 PNG, 낱장 프레임 (시트 아님)
- [ ] **5개 모션** 폴더 · 프레임 수 일치 (Idle 18 / Walking 24 / Slashing 12 / Hurt 12 / Dying 15)
- [ ] 4종족 각각 독립 폴더 · 파일명 규칙 (`0_[Name]_[Motion]_000` ~, 3자리 0패딩)
- [ ] 전 프레임 피벗 정렬 일치 (중심 기준)
- [ ] 치비 비율 · HD 스무딩 (픽셀아트 아님) · 주인공과 화풍 통일
- [ ] 역할별 실루엣 구분 명확 (전투 가독성)
- [ ] 리그 파일이 아닌 **구운 PNG 프레임**으로 납품

---

## 부록 — 코드 후속 (내부용 · 아트팀 무관)

> 새 에셋 입고 시점에 처리(Sonnet). 지금은 임시 해골 에셋이 살아 있어 보류.

- `SkeletonWarrior.gd:_build_sprite_frames()` — 경로 `assets/characters/skeleton_warrior_%d/` · 접두 `0_Skeleton_Warrior` 가 **하드코딩**. 신규 역할 폴더명(`Warrior`/`Archer`/`Tank`/`Bomber`)·접두(`0_[Role]`)로 교체 필요.
- `SkeletonWarrior.gd:TYPE_PRESETS` 의 `variant` — 현재 warrior=1 / archer=2 / tank=3 / **bomber=1(전사 아트 재사용 중)**. **폴더명이 코드 타입(warrior/archer/tank/bomber)과 1:1 일치**하므로, `variant` 정수 매핑을 제거하고 **타입명 → 폴더명을 그대로** 쓰면 됨(폭탄병 독립 아트 반영, 매핑 테이블 불필요).
- 진화 잔재(`_evolve`/`evolves`/`kill_count`, MD6 폐기) 제거와 함께 정리하면 깔끔.

## 관련 문서
- [아트의뢰_주인공_초보마왕.md](./아트의뢰_주인공_초보마왕.md) — 주인공 아트 의뢰(동일 규격·화풍 기준)
- [MainPlay/03_하인_소환/README.md](./MainPlay/03_하인_소환/README.md) — 하인 시스템 설계 정본(MD1~MD11)
- [세계관_기획.md](./세계관_기획.md) — 세계관 단일 진실
